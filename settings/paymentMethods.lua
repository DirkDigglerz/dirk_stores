return {
  ['bank'] = {
    name = 'Bank',
    icon = 'user',

    add = function(player, amount)
      --// Implement your own bank system here
      return true
    end,

    remove = function(player, amount)
      --// Implement your own bank system here
      
      return true
    end
  },
  ['cash'] = {
    name = 'Cash',
    icon = 'user',

    add = function(player, amount)
      --// Implement your own bank system here
      return true
    end,

    remove = function(player, amount)
      --// Implement your own bank system here
      
      return true
    end
  },
  ['card'] = {
    name = 'Card',
    icon = 'user',

    add = function(player, amount)
      --// Implement your own bank system here
      return true
    end,

    remove = function(player, amount)
      --// Implement your own bank system here
      
      return true
    end
  },
  
}